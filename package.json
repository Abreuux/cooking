{
  "name": "necotium-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "python app.py",
    "build": "cd frontend && npm run build",
    "deploy": "firebase deploy"
  },
  "dependencies": {
    "firebase-tools": "^12.0.0"
  }
} 